= Angular =

	* [[Firebase]]

== Angular CLI ==

Common `ng new` options
	* --skip-git=true
	* --skip-tests=true
	* --skip-install=true
	* --prefix app-shell

== RxJS ==

    * http://reactivex.io/rxjs/manual/overview.html
    * https://codepen.io/yfain/pen/xLaXWx?editors=1012
    * https://codepen.io/yfain/pen/EveBNP?editors=1012

    * catchError: Remember to return an observable from the catchError function

== In memory web api ==

	* https://github.com/angular/in-memory-web-api

== Semantic UI ==

https://dev.to/chiangs/integrating-semantic-ui-to-an-angular-cli-project-20el

Need (in addition to node, angular-cli):
	* gulp
	* semantic-ui
	* update .angular_cli.json (instructions above missing jquery)

```
      "styles": [
        "../semantic/dist/semantic.min.css",
        "styles.css"
      ],
      "scripts": [
        "../node_modules/jquery/dist/jquery.min.js",
        "../semantic/dist/semantic.min.js"
      ],
```

Themes - don’t forget to run $ gulp build again when you make a change.

Maybe just stick with npm as the package manager.

Quirks

`yarn global add gulp-cli` does not install binaries properly.
Use `npm install -g gulp-cli`

Installing Semantic UI is also weird. Loops with yarn. Probably
fine with npm but I've been using yarn for this project. So...

from https://github.com/yarnpkg/yarn/issues/976
```
> yarn add semantic-ui --ignore-scripts
> cd ./node_modules/semantic-ui/
> gulp install
```
